# TODO: Install the correct Crossplane provider for GCP Cloud SQL
# Find the right package on: https://marketplace.upbound.io/providers
#
# The resource kind is: Provider
# API group: pkg.crossplane.io/v1
#
# You need to find:
#   - The correct package image (hint: look for "provider-gcp-cloudsql")
#   - The right version to use

apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: # TODO
spec:
  package: # TODO
