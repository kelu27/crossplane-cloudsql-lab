# TODO: Configure the GCP provider with your project credentials
#
# Documentation: https://marketplace.upbound.io/providers/upbound/provider-gcp-cloudsql
#
# You need to:
#   1. Set the correct apiVersion for this ProviderConfig
#      (hint: it depends on the provider you installed)
#   2. Fill in your GCP project ID
#   3. Reference the Kubernetes secret created by the setup script
#      The secret name is: gcp-credentials
#      The key inside the secret is: creds
#      The namespace is: crossplane-system

apiVersion: # TODO
kind: ProviderConfig
metadata:
  name: default
spec:
  projectID: # TODO
  credentials:
    source: Secret
    secretRef:
      namespace: # TODO
      name: # TODO
      key: # TODO