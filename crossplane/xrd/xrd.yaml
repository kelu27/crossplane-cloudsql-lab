# TODO: Define a CompositeResourceDefinition (XRD)
#
# Documentation: https://docs.crossplane.io/latest/composition/composite-resource-definitions/
#
# Requirements:
#   - apiVersion: apiextensions.crossplane.io/v2
#   - scope: Cluster
#   - group: db.example.org
#   - Composite kind: XPostgreSQLInstance
#   - Version: v1alpha1
#   - The spec must expose a `parameters` object containing:
#       * region (string, required)
#       * tier (string, optional — default: "db-f1-micro")
#       * databaseVersion (string, required or defaulted so upgrades are declarative)
#       * databaseName (string, required or defaulted)
#       * userName (string, required or defaulted)
#       * a way to provide the database user password, including the Secret namespace
#   - The schema must allow the XR to set spec.writeConnectionSecretToRef.name and .namespace
#   - Do not add the legacy namespaced interface on this v2 branch
#
# Tips:
#   - The XRD schema lives under spec.versions[].schema.openAPIV3Schema
#   - The resulting API should be a cluster-scoped XPostgreSQLInstance resource
#   - Use defaults where that makes the student resource easier to write
#
# Write your XRD below: